---
import Popover from "./popover.astro";
import Video from "./video.astro";
export interface ChecklistItem {
  title: string;
  description: string;
  icon?: string; 

}

interface Props {
  tag?: string;
  title: string;
  description?: string;
  items?: ChecklistItem[];
  direction?: "left" | "right";
  variant?: "dark" | "yellow" | "navy";
  imageSrc?: string;
  imageAlt?: string;
  class?: string;
  isVideo?: boolean;
  videoSrc?: string;
}

const {
  tag,
  title,
  description,
  items = [],
  direction = "right",
  variant = "dark",
  imageSrc,
  imageAlt = "Feature visual",
  class: className = "",
  isVideo = false,
  videoSrc = "",
} = Astro.props;

const variantStyles = {
  dark: {
    bg: "bg-[#18181b]",
    tag: "text-zinc-400",
    title: "text-(--neutral)",
    desc: "text-zinc-400",
    itemTitle: "text-white",
    itemDesc: "text-zinc-400",
    badge: "border-zinc-700 bg-zinc-800 text-zinc-300",
  },
  yellow: {
    bg: "bg-[#18181b]",
    tag: "text-zinc-400",
    title: "text-(--secondary)",
    desc: "text-zinc-400",
    itemTitle: "text-(--secondary)",
    itemDesc: "text-zinc-400",
    badge: "border-(--secondary)/30 bg-(--secondary) text-zinc-950",
  },
  navy: {
    bg: "bg-[#0d1527]",
    tag: "text-zinc-400",
    title: "text-(--secondary)",
    desc: "text-zinc-300",
    itemTitle: "text-(--secondary)",
    itemDesc: "text-zinc-400",
    badge: "border-(--secondary)/30 bg-(--secondary) text-zinc-950",
  },
}[variant];

const isImageLeft = direction === "left";
---

<section id={tag}
  class={`relative z-50 overflow-visible p-8 md:p-14 transition-colors ${variantStyles.bg} ${className}`}
>
  <div
    class={`grid grid-cols-1 items-center gap-10 lg:grid-cols-12 lg:gap-16 ${
      isImageLeft ? "lg:[&>*:first-child]:order-2 lg:[&>*:last-child]:order-1" : ""
    }`}
  >
    <!-- Bloque de Texto y Lista de Features -->
    <div class="flex flex-col justify-center space-y-8 lg:col-span-6">
      
      <!-- Encabezado -->
      <div class="space-y-3">
        {
          tag && (
            <span class={`text-sm font-semibold tracking-wider ${variantStyles.tag}`}>
              {tag}
            </span>
          )
        }
        <h2 class={`text-3xl font-extrabold tracking-tight sm:text-4xl md:text-5xl ${variantStyles.title}`}>
          {title}
        </h2>
        {
          description && (
            <p class={`text-sm sm:text-base leading-relaxed ${variantStyles.desc}`}>
              {description}
            </p>
          )
        }
      </div>

      <!-- Lista vertical con checks dinámicos -->
      {
        items.length > 0 && (
          <div class="space-y-4 pt-2">
            {items.map((item) => (
              <div class="flex items-start gap-3.5">
                

                <!-- Título y Texto secundario -->
                <div>
                  <Popover title={item.title} description={item.description} 
                  image={item.icon || "public/img/logo.png"}>
                  <h4 class={`text-lg sm:text-sm font-semibold leading-none z-0 ${variantStyles.itemTitle}`}>
                    {item.title}
                  </h4>      
                  </Popover>            
                </div>
              </div>
            ))}
          </div>
        )
      }

      <slot name="actions" />
    </div>

    <!-- Bloque de Imagen / Visual -->
    <div class="relative lg:col-span-6">
      <div
        class="relative aspect-16/10 w-full overflow-hidden rounded-3xl border border-zinc-800/60 bg-zinc-900 shadow-xl"
      >
        <slot name="media">
          {
            isVideo ? (
              <Video
                src={videoSrc}
                poster="public/img/video-poster.jpg"
                muted
                autoPlay={true}
                loop
                class="aspect-video rounded-2xl"
              />
            ) : (
              imageSrc ? (
                <img
                  src={imageSrc}
                  alt={imageAlt}
                  class="h-full w-full object-cover transition-transform duration-500 hover:scale-105"
                  loading="lazy"
                />
              ) : (
                //<!-- Placeholder Figma Grid -->
                <div class="h-full w-full bg-[radial-gradient(#27272a_1px,transparent_1px)] [bg-size:16px_16px] bg-zinc-900/90 flex items-center justify-center">
                  <span class="text-xs text-zinc-600 font-medium">Image / Asset Placeholder</span>
                </div>
              )
            )
          }
        </slot>
      </div>
    </div>

  </div>
</section>